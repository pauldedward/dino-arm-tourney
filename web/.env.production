# Loaded by `next build` / `next start`. Committed, non-secret only.
# Secrets for prod go in .env.production.local (gitignored) or hosting-provider env vars.
NEXT_PUBLIC_APP_URL=https://dino-arm-tourney.example.com
NEXT_PUBLIC_APP_ENV=production
